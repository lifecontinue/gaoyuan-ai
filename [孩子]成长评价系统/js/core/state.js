/* =========================================================
   core/state.js — 全局应用状态（当前阶段）
   单一可变状态源；由 main.js 初始化、router.setStage 更新。
   领域层以只读方式引用本对象，保证「单一数据源」。
   ========================================================= */

export const state = {
  /** 当前阶段：'k' 幼儿园 / 'p' 小学 */
  stage: 'k'
};
